module.exports =
/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/dist/";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ({

/***/ 0:
/***/ (function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(330);


/***/ }),

/***/ 3:
/***/ (function(module, exports) {

	/* globals __VUE_SSR_CONTEXT__ */

	// this module is a runtime utility for cleaner component module output and will
	// be included in the final webpack user bundle

	module.exports = function normalizeComponent (
	  rawScriptExports,
	  compiledTemplate,
	  injectStyles,
	  scopeId,
	  moduleIdentifier /* server only */
	) {
	  var esModule
	  var scriptExports = rawScriptExports = rawScriptExports || {}

	  // ES6 modules interop
	  var type = typeof rawScriptExports.default
	  if (type === 'object' || type === 'function') {
	    esModule = rawScriptExports
	    scriptExports = rawScriptExports.default
	  }

	  // Vue.extend constructor export interop
	  var options = typeof scriptExports === 'function'
	    ? scriptExports.options
	    : scriptExports

	  // render functions
	  if (compiledTemplate) {
	    options.render = compiledTemplate.render
	    options.staticRenderFns = compiledTemplate.staticRenderFns
	  }

	  // scopedId
	  if (scopeId) {
	    options._scopeId = scopeId
	  }

	  var hook
	  if (moduleIdentifier) { // server build
	    hook = function (context) {
	      // 2.3 injection
	      context =
	        context || // cached call
	        (this.$vnode && this.$vnode.ssrContext) || // stateful
	        (this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext) // functional
	      // 2.2 with runInNewContext: true
	      if (!context && typeof __VUE_SSR_CONTEXT__ !== 'undefined') {
	        context = __VUE_SSR_CONTEXT__
	      }
	      // inject component styles
	      if (injectStyles) {
	        injectStyles.call(this, context)
	      }
	      // register component module identifier for async chunk inferrence
	      if (context && context._registeredComponents) {
	        context._registeredComponents.add(moduleIdentifier)
	      }
	    }
	    // used by ssr in case component is cached and beforeCreate
	    // never gets called
	    options._ssrRegister = hook
	  } else if (injectStyles) {
	    hook = injectStyles
	  }

	  if (hook) {
	    var functional = options.functional
	    var existing = functional
	      ? options.render
	      : options.beforeCreate
	    if (!functional) {
	      // inject component registration as beforeCreate hook
	      options.beforeCreate = existing
	        ? [].concat(existing, hook)
	        : [hook]
	    } else {
	      // register for functioal component in vue file
	      options.render = function renderWithStyleInjection (h, context) {
	        hook.call(context)
	        return existing(h, context)
	      }
	    }
	  }

	  return {
	    esModule: esModule,
	    exports: scriptExports,
	    options: options
	  }
	}


/***/ }),

/***/ 10:
/***/ (function(module, exports) {

	/*
		MIT License http://www.opensource.org/licenses/mit-license.php
		Author Tobias Koppers @sokra
	*/
	// css base code, injected by the css-loader
	module.exports = function() {
		var list = [];

		// return the list of modules as css string
		list.toString = function toString() {
			var result = [];
			for(var i = 0; i < this.length; i++) {
				var item = this[i];
				if(item[2]) {
					result.push("@media " + item[2] + "{" + item[1] + "}");
				} else {
					result.push(item[1]);
				}
			}
			return result.join("");
		};

		// import a list of modules into the list
		list.i = function(modules, mediaQuery) {
			if(typeof modules === "string")
				modules = [[null, modules, ""]];
			var alreadyImportedModules = {};
			for(var i = 0; i < this.length; i++) {
				var id = this[i][0];
				if(typeof id === "number")
					alreadyImportedModules[id] = true;
			}
			for(i = 0; i < modules.length; i++) {
				var item = modules[i];
				// skip already imported module
				// this implementation is not 100% perfect for weird media query combinations
				//  when a module is imported multiple times with different media queries.
				//  I hope this will never occur (Hey this way we have smaller bundles)
				if(typeof item[0] !== "number" || !alreadyImportedModules[item[0]]) {
					if(mediaQuery && !item[2]) {
						item[2] = mediaQuery;
					} else if(mediaQuery) {
						item[2] = "(" + item[2] + ") and (" + mediaQuery + ")";
					}
					list.push(item);
				}
			}
		};
		return list;
	};


/***/ }),

/***/ 11:
/***/ (function(module, exports, __webpack_require__) {

	/*
	  MIT License http://www.opensource.org/licenses/mit-license.php
	  Author Tobias Koppers @sokra
	  Modified by Evan You @yyx990803
	*/

	var hasDocument = typeof document !== 'undefined'

	if (false) {
	  if (!hasDocument) {
	    throw new Error(
	    'vue-style-loader cannot be used in a non-browser environment. ' +
	    "Use { target: 'node' } in your Webpack config to indicate a server-rendering environment."
	  ) }
	}

	var listToStyles = __webpack_require__(12)

	/*
	type StyleObject = {
	  id: number;
	  parts: Array<StyleObjectPart>
	}

	type StyleObjectPart = {
	  css: string;
	  media: string;
	  sourceMap: ?string
	}
	*/

	var stylesInDom = {/*
	  [id: number]: {
	    id: number,
	    refs: number,
	    parts: Array<(obj?: StyleObjectPart) => void>
	  }
	*/}

	var head = hasDocument && (document.head || document.getElementsByTagName('head')[0])
	var singletonElement = null
	var singletonCounter = 0
	var isProduction = false
	var noop = function () {}

	// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
	// tags it will allow on a page
	var isOldIE = typeof navigator !== 'undefined' && /msie [6-9]\b/.test(navigator.userAgent.toLowerCase())

	module.exports = function (parentId, list, _isProduction) {
	  isProduction = _isProduction

	  var styles = listToStyles(parentId, list)
	  addStylesToDom(styles)

	  return function update (newList) {
	    var mayRemove = []
	    for (var i = 0; i < styles.length; i++) {
	      var item = styles[i]
	      var domStyle = stylesInDom[item.id]
	      domStyle.refs--
	      mayRemove.push(domStyle)
	    }
	    if (newList) {
	      styles = listToStyles(parentId, newList)
	      addStylesToDom(styles)
	    } else {
	      styles = []
	    }
	    for (var i = 0; i < mayRemove.length; i++) {
	      var domStyle = mayRemove[i]
	      if (domStyle.refs === 0) {
	        for (var j = 0; j < domStyle.parts.length; j++) {
	          domStyle.parts[j]()
	        }
	        delete stylesInDom[domStyle.id]
	      }
	    }
	  }
	}

	function addStylesToDom (styles /* Array<StyleObject> */) {
	  for (var i = 0; i < styles.length; i++) {
	    var item = styles[i]
	    var domStyle = stylesInDom[item.id]
	    if (domStyle) {
	      domStyle.refs++
	      for (var j = 0; j < domStyle.parts.length; j++) {
	        domStyle.parts[j](item.parts[j])
	      }
	      for (; j < item.parts.length; j++) {
	        domStyle.parts.push(addStyle(item.parts[j]))
	      }
	      if (domStyle.parts.length > item.parts.length) {
	        domStyle.parts.length = item.parts.length
	      }
	    } else {
	      var parts = []
	      for (var j = 0; j < item.parts.length; j++) {
	        parts.push(addStyle(item.parts[j]))
	      }
	      stylesInDom[item.id] = { id: item.id, refs: 1, parts: parts }
	    }
	  }
	}

	function createStyleElement () {
	  var styleElement = document.createElement('style')
	  styleElement.type = 'text/css'
	  head.appendChild(styleElement)
	  return styleElement
	}

	function addStyle (obj /* StyleObjectPart */) {
	  var update, remove
	  var styleElement = document.querySelector('style[data-vue-ssr-id~="' + obj.id + '"]')

	  if (styleElement) {
	    if (isProduction) {
	      // has SSR styles and in production mode.
	      // simply do nothing.
	      return noop
	    } else {
	      // has SSR styles but in dev mode.
	      // for some reason Chrome can't handle source map in server-rendered
	      // style tags - source maps in <style> only works if the style tag is
	      // created and inserted dynamically. So we remove the server rendered
	      // styles and inject new ones.
	      styleElement.parentNode.removeChild(styleElement)
	    }
	  }

	  if (isOldIE) {
	    // use singleton mode for IE9.
	    var styleIndex = singletonCounter++
	    styleElement = singletonElement || (singletonElement = createStyleElement())
	    update = applyToSingletonTag.bind(null, styleElement, styleIndex, false)
	    remove = applyToSingletonTag.bind(null, styleElement, styleIndex, true)
	  } else {
	    // use multi-style-tag mode in all other cases
	    styleElement = createStyleElement()
	    update = applyToTag.bind(null, styleElement)
	    remove = function () {
	      styleElement.parentNode.removeChild(styleElement)
	    }
	  }

	  update(obj)

	  return function updateStyle (newObj /* StyleObjectPart */) {
	    if (newObj) {
	      if (newObj.css === obj.css &&
	          newObj.media === obj.media &&
	          newObj.sourceMap === obj.sourceMap) {
	        return
	      }
	      update(obj = newObj)
	    } else {
	      remove()
	    }
	  }
	}

	var replaceText = (function () {
	  var textStore = []

	  return function (index, replacement) {
	    textStore[index] = replacement
	    return textStore.filter(Boolean).join('\n')
	  }
	})()

	function applyToSingletonTag (styleElement, index, remove, obj) {
	  var css = remove ? '' : obj.css

	  if (styleElement.styleSheet) {
	    styleElement.styleSheet.cssText = replaceText(index, css)
	  } else {
	    var cssNode = document.createTextNode(css)
	    var childNodes = styleElement.childNodes
	    if (childNodes[index]) styleElement.removeChild(childNodes[index])
	    if (childNodes.length) {
	      styleElement.insertBefore(cssNode, childNodes[index])
	    } else {
	      styleElement.appendChild(cssNode)
	    }
	  }
	}

	function applyToTag (styleElement, obj) {
	  var css = obj.css
	  var media = obj.media
	  var sourceMap = obj.sourceMap

	  if (media) {
	    styleElement.setAttribute('media', media)
	  }

	  if (sourceMap) {
	    // https://developer.chrome.com/devtools/docs/javascript-debugging
	    // this makes source maps inside style tags work properly in Chrome
	    css += '\n/*# sourceURL=' + sourceMap.sources[0] + ' */'
	    // http://stackoverflow.com/a/26603875
	    css += '\n/*# sourceMappingURL=data:application/json;base64,' + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + ' */'
	  }

	  if (styleElement.styleSheet) {
	    styleElement.styleSheet.cssText = css
	  } else {
	    while (styleElement.firstChild) {
	      styleElement.removeChild(styleElement.firstChild)
	    }
	    styleElement.appendChild(document.createTextNode(css))
	  }
	}


/***/ }),

/***/ 12:
/***/ (function(module, exports) {

	/**
	 * Translates the list format produced by css-loader into something
	 * easier to manipulate.
	 */
	module.exports = function listToStyles (parentId, list) {
	  var styles = []
	  var newStyles = {}
	  for (var i = 0; i < list.length; i++) {
	    var item = list[i]
	    var id = item[0]
	    var css = item[1]
	    var media = item[2]
	    var sourceMap = item[3]
	    var part = {
	      id: parentId + ':' + i,
	      css: css,
	      media: media,
	      sourceMap: sourceMap
	    }
	    if (!newStyles[id]) {
	      styles.push(newStyles[id] = { id: id, parts: [part] })
	    } else {
	      newStyles[id].parts.push(part)
	    }
	  }
	  return styles
	}


/***/ }),

/***/ 65:
/***/ (function(module, exports) {

	module.exports = require("vue");

/***/ }),

/***/ 294:
/***/ (function(module, exports) {

	'use strict';

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	var hasOwn = Object.prototype.hasOwnProperty;
	var toStr = Object.prototype.toString;

	var isArray = function isArray(arr) {
		if (typeof Array.isArray === 'function') {
			return Array.isArray(arr);
		}

		return toStr.call(arr) === '[object Array]';
	};

	var isPlainObject = function isPlainObject(obj) {
		if (!obj || toStr.call(obj) !== '[object Object]') {
			return false;
		}

		var hasOwnConstructor = hasOwn.call(obj, 'constructor');
		var hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn.call(obj.constructor.prototype, 'isPrototypeOf');
		// Not own constructor property must be Object
		if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) {
			return false;
		}

		// Own properties are enumerated firstly, so to speed up,
		// if last one is own, then all properties are own.
		var key;
		for (key in obj) {/**/}

		return typeof key === 'undefined' || hasOwn.call(obj, key);
	};

	module.exports = function extend() {
		var options, name, src, copy, copyIsArray, clone;
		var target = arguments[0];
		var i = 1;
		var length = arguments.length;
		var deep = false;

		// Handle a deep copy situation
		if (typeof target === 'boolean') {
			deep = target;
			target = arguments[1] || {};
			// skip the boolean and the target
			i = 2;
		}
		if (target == null || (typeof target === 'undefined' ? 'undefined' : _typeof(target)) !== 'object' && typeof target !== 'function') {
			target = {};
		}

		for (; i < length; ++i) {
			options = arguments[i];
			// Only deal with non-null/undefined values
			if (options != null) {
				// Extend the base object
				for (name in options) {
					src = target[name];
					copy = options[name];

					// Prevent never-ending loop
					if (target !== copy) {
						// Recurse if we're merging plain objects or arrays
						// debugger;
						if (deep && copy && (isPlainObject(copy) || (copyIsArray = isArray(copy)))) {
							if (copyIsArray) {
								copyIsArray = false;
								clone = src && isArray(src) ? src : [];
								if (isArray(src)) {
									target[name] = src.concat(copy);
								} else {
									target[name] = extend(deep, clone, copy);
								}
							} else {
								clone = src && isPlainObject(src) ? src : {};
								target[name] = extend(deep, clone, copy);
							}

							// Never move original objects, clone them


							// Don't bring in undefined values
						} else if (typeof copy !== 'undefined') {
							target[name] = copy;
						}
					}
				}
			}
		}

		// Return the modified object
		return target;
	};

/***/ }),

/***/ 330:
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	var _template = __webpack_require__(331);

	var _template2 = _interopRequireDefault(_template);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	/* istanbul ignore next */
	_template2.default.install = function (Vue) {
	  Vue.component(_template2.default.name, _template2.default);
	};

	exports.default = _template2.default;

/***/ }),

/***/ 331:
/***/ (function(module, exports, __webpack_require__) {

	function injectStyle (ssrContext) {
	  __webpack_require__(332)
	}
	var Component = __webpack_require__(3)(
	  /* script */
	  __webpack_require__(334),
	  /* template */
	  __webpack_require__(336),
	  /* styles */
	  injectStyle,
	  /* scopeId */
	  null,
	  /* moduleIdentifier (server only) */
	  null
	)

	module.exports = Component.exports


/***/ }),

/***/ 332:
/***/ (function(module, exports, __webpack_require__) {

	// style-loader: Adds some css to the DOM by adding a <style> tag

	// load the styles
	var content = __webpack_require__(333);
	if(typeof content === 'string') content = [[module.id, content, '']];
	if(content.locals) module.exports = content.locals;
	// add the styles to the DOM
	var update = __webpack_require__(11)("7628cf78", content, true);

/***/ }),

/***/ 333:
/***/ (function(module, exports, __webpack_require__) {

	exports = module.exports = __webpack_require__(10)();
	// imports


	// module
	exports.push([module.id, "\n.yon-uitemplate {\r\n  background: white;\n}\r\n", ""]);

	// exports


/***/ }),

/***/ 334:
/***/ (function(module, exports, __webpack_require__) {

	"use strict";

	exports.__esModule = true;

	var _vue = __webpack_require__(65);

	var _vue2 = _interopRequireDefault(_vue);

	var _extend = __webpack_require__(294);

	var _extend2 = _interopRequireDefault(_extend);

	var _templateMixin = __webpack_require__(335);

	var _templateMixin2 = _interopRequireDefault(_templateMixin);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	exports.default = {
	  name: "IfbpTemplate",
	  mixins: [_templateMixin2.default],
	  props: {
	    tplId: {
	      type: String,
	      default: (new Date().getTime() + Math.random() + "").replace(".", "")
	    },
	    tplCode: String,
	    pkTemp: String,
	    funnode: String,
	    nexuskey: String,
	    formCancelFun: String,
	    formConfirmFun: String,
	    showType: {
	      type: String
	    },
	    formPosition: {
	      type: String,
	      default: "ahead"
	    },
	    editable: {
	      type: Boolean,
	      default: true
	    },
	    tplResetFun: {
	      type: Function,
	      default: function _default($node) {
	        var str = $node[0].outerHTML;
	        return str;
	      }
	    },
	    tplFinalResetFun: {
	      type: Function,
	      default: function _default($node) {
	        var str = $node[0].outerHTML;
	        return str;
	      }
	    },
	    tplData: [Object, Function],
	    query_uitemplate_url: "",
	    mode: "normal"
	  },
	  data: function data() {
	    return {
	      formRef: "formRef",
	      formModel: "",
	      tableRef: "tableRef",
	      tableModel: "",
	      tableFormRef: "tableFormRef",
	      tableShow: true,
	      formShow: false
	    };
	  },
	  created: function created() {
	    if (this.ifbp_configs) {
	      this.mode = this.ifbp_configs.mode || this.mode;
	      this.query_uitemplate_url = this.ifbp_configs.query_uitemplate_url;
	    }
	  },
	  mounted: function mounted() {
	    this.request();
	  },

	  watch: {
	    tplData: function tplData(newData) {
	      if (this.comp) {
	        for (var i in newData) {
	          this.comp[i] = newData[i];
	        }
	      }
	    },
	    editable: function editable(isEditFlag) {
	      this.comp && (this.comp.editable = isEditFlag);
	    },
	    formShow: function formShow(_formShow) {
	      this.comp && (this.comp.formShow = _formShow);
	    },
	    tableShow: function tableShow(_tableShow) {
	      this.comp && (this.comp.tableShow = _tableShow);
	    }
	  },
	  methods: {
	    requestSucessFun: function requestSucessFun(res) {
	      var oThis = this;
	      var res = JSON.stringify(res.data);
	      // debugger;
	      res = res.replace(/v-select=\\"\\"/g, "").replace(/v-dnd=\\"\\"/g, "").replace(/v-drop=\\"\\"/g, "").replace(/v-drop=\\"{muilt:true}\\"/g, "").replace(/v-drop=\\"{muilt: true}\\"/g, "").replace(/v-comp=\\"\\"/g, "");
	      var resObj = JSON.parse(res);
	      if (resObj.formVersion !== "1" && resObj.formVersion !== 1) {
	        return;
	      }
	      // 处理dom结构
	      var resTemplate = resObj.formLayout;
	      var $dom = $(resTemplate);
	      resTemplate = this.tplResetFun.call(this, $dom);
	      var hasForm = $dom.find("#form_area").children().length > 0;
	      var hasTable = $dom.find("#table_area").children().length > 0;
	      if (!this.showType) {
	        if (hasForm && !hasTable) {
	          this.showType = "form";
	        } else if (!hasForm && hasTable) {
	          this.showType = "table";
	        } else if (hasForm && hasTable) {
	          this.showType = "table-form";
	        }
	      }

	      // 删除需要隐藏的dom
	      $dom.find("[hide=true]").remove();
	      if (this.showType === "form" || this.showType === "table-form") {
	        var formHTML = $dom.find("#form_area").html();
	        var $formDom = $(formHTML);
	        $formDom.attr(":adaptation", "true");
	        $formDom.attr("v-show", "formShow");
	        var nowFormRef = $formDom.attr("ref");
	        if (nowFormRef) {
	          this.formRef = nowFormRef;
	        } else {
	          $formDom.attr("ref", this.formRef);
	        }
	        this.formModel = $formDom.attr(":model");
	        formHTML = $formDom[0].outerHTML;
	        $formDom.attr("ref", this.tableFormRef);
	        $formDom.removeAttr("v-show");
	        var tableFormHTML = $formDom[0].outerHTML;
	      }
	      if (this.showType === "table" || this.showType === "table-form") {
	        var tableHTML = $dom.find("#table_area").html();
	        var $tableDom = $(tableHTML);
	        $tableDom.attr("v-show", "tableShow");
	        var nowTableRef = $tableDom.attr("ref");
	        if (nowTableRef) {
	          this.tableRef = nowTableRef;
	        } else {
	          $tableDom.attr("ref", this.tableRef);
	        }
	        this.tableModel = $tableDom.attr(":data");
	        $tableDom.attr("v-on:selection-change", "handleSelectionChange");
	        $tableDom.attr("v-on:select-all", "handleSelectAll");
	        $tableDom.attr("v-on:row-click", "handleRowClick");

	        tableHTML = $tableDom[0].outerHTML;
	      }

	      if (this.showType === "form") {
	        resTemplate = formHTML;
	        this.formShow = true;
	        this.tableShow = false;
	      } else if (this.showType === "table") {
	        resTemplate = tableHTML;
	        this.formShow = false;
	        this.tableShow = true;
	      } else if (this.showType === "table-form") {
	        var formButtonHTML = '<div class="form-button-div" v-show="formShow">' + ' <el-button type="default" class="button-no-radius" @click="formCancelClick(\'form\')">取消</el-button>' + ' <el-button type="primary" class="button-no-radius" @click="formConfirmClick(\'form\')">保存</el-button>' + "</div>";
	        var tableFormButtonHTML = '<div class="form-button-div" >' + ' <el-button type="default" class="button-no-radius" @click="formCancelClick(\'table-form\')">取消</el-button>' + ' <el-button type="primary" class="button-no-radius" @click="formConfirmClick(\'table-form\')">保存</el-button>' + "</div>";
	        $tableDom.prepend('<el-table-column  type="noIconExpand"><template scope="props">' + tableFormHTML + tableFormButtonHTML + "</template></el-table-column>");
	        if (this.formPosition === "ahead") {
	          resTemplate = "<div>" + formHTML + formButtonHTML + $tableDom[0].outerHTML + "</div>";
	        } else {
	          resTemplate = "<div>" + $tableDom[0].outerHTML + "<div style='height:20px;position:relative;'></div>" + formHTML + formButtonHTML + "</div>";
	        }
	      }
	      var $finalDom = $(resTemplate);
	      resTemplate = this.tplFinalResetFun.call(this, $finalDom);
	      // extend处理时如果是数组执行合并操作
	      if (typeof this.tplData === "function") {
	        var baseData = this.tplData.call(this);
	      } else {
	        var baseData = this.tplData;
	      }
	      if (baseData.uitemplateTableData) {
	        baseData[tableModel] = baseData.uitemplateTableData;
	        delete baseData.uitemplateTableData;
	      }
	      if (baseData.uitemplateFormData) {
	        baseData[formModel] = baseData.uitemplateFormData;
	        delete baseData.uitemplateFormData;
	      }

	      this.resBaseData = JSON.parse(JSON.stringify(resObj.formData));
	      var resData = (0, _extend2.default)(true, {}, resObj.formData, baseData, {
	        editable: this.editable,
	        formShow: this.formShow,
	        tableShow: this.tableShow
	      });

	      var baseProps = {
	        template: resTemplate,
	        data: function data() {
	          return resData;
	        }
	      };
	      var otherProps = this.$vnode.data.attrs;
	      otherProps.methods = otherProps.methods || {};
	      otherProps.methods.handleSelectionChange = function (selection) {
	        oThis.$emit("selection-change", selection);
	      };
	      otherProps.methods.handleSelectAll = function (selection) {
	        oThis.$emit("select-all", selection);
	      };
	      otherProps.methods.handleRowClick = function (row, event, column) {
	        oThis.$emit("row-click", row, event, column);
	      };

	      otherProps.methods.getTableComp = function () {
	        return this.$refs[oThis.tableRef];
	      };
	      otherProps.methods.getFormComp = function () {
	        return this.$refs[oThis.formRef];
	      };
	      otherProps.methods.getTableFormComp = function () {
	        return this.$refs[oThis.tableFormRef];
	      };
	      otherProps.methods.validate = function (callback, type) {
	        var _this = this;

	        if (type === "form" || !type) {
	          var ref = oThis.formRef;
	        } else {
	          var ref = oThis.tableFormRef;
	        }
	        this.$refs[ref].validate(function (valid) {
	          callback.call(_this, valid);
	        });
	      };

	      otherProps.methods.resetFormData = function (type) {
	        this[oThis.formModel] = JSON.parse(JSON.stringify(oThis.resBaseData[oThis.formModel]));
	      };

	      otherProps.methods.handleClick = function (icon, scope) {
	        oThis.$emit(icon + "-table-click", scope);
	      };

	      otherProps.methods.formCancelClick = function (type) {
	        oThis.$emit("form-cancel-click", type);
	      };

	      otherProps.methods.formConfirmClick = function (type) {
	        oThis.$emit("form-confirm-click", type);
	      };

	      var props = (0, _extend2.default)(true, {}, otherProps, baseProps);
	      var comp = _vue2.default.extend(props);
	      this.comp = new comp();
	      var compMount = this.comp.$mount();
	      this.$el.appendChild(compMount.$el);
	      this.comp.$nextTick(function () {
	        if (oThis.tplData && oThis.tplData.ifxbptxempxlatxetaxblxemxodxel) {
	          oThis.setTableData(oThis.tplData.ifxbptxempxlatxetaxblxemxodxel);
	          delete oThis.tplData.ifxbptxempxlatxetaxblxemxodxel;
	        }
	        if (oThis.tplData && oThis.tplData.ifxbptxempxlatxefoxrmxemxodxel) {
	          oThis.setFormData(oThis.tplData.ifxbptxempxlatxefoxrmxemxodxel);
	          delete oThis.tplData.ifxbptxempxlatxefoxrmxemxodxel;
	        }
	        oThis.$emit("after-create", oThis);
	      });
	    },
	    request: function request() {
	      var _this2 = this;

	      if (this.tplCode) {
	        this.$http({
	          url: "/uitemplate_web/uitemplate_ctr/uitemplate_design_ctr/queryTemplateByCode",
	          method: "post",
	          headers: {
	            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8"
	          },
	          data: this.geturlencode(this.tplCode)
	        }).then(function (res) {
	          _this2.requestSucessFun(res);
	        }).catch(function (res) {
	          console.log("err:" + res.message);
	          _this2.$message({
	            showClose: true,
	            message: "UI模板获取结构数据失败",
	            type: "error"
	          });
	        });
	      } else if (this.pkTemp) {
	        this.$http({
	          url: "/uitemplate_web/uitemplate_ctr/uitemplate_design_ctr/queryTemplate?" + this.geturlencode({ pk_temp: this.pkTemp }),
	          method: "get",
	          headers: {
	            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8"
	          }
	        }).then(function (res) {
	          _this2.requestSucessFun(res);
	        }).catch(function (res) {
	          console.log("err:" + res.message);
	          _this2.$message({
	            showClose: true,
	            message: "UI模板获取结构数据失败",
	            type: "error"
	          });
	        });
	      } else if (this.funnode && this.nexuskey) {
	        var d, url, headers, data;
	        if (this.mode == "design") {
	          d = {
	            funcnode: this.funnode,
	            nexuskey: this.nexuskey
	          };
	          url = this.query_uitemplate_url;
	          headers = {
	            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8"
	          };
	          data = this.geturlencode(d);
	        } else {
	          d = {
	            funnode: this.funnode,
	            nexusKey: this.nexuskey
	          };
	          url = "/wbalone/ieoproleuitemplate/listByFunNodeAndNexusKey";
	          headers = {
	            "Content-Type": "application/json"
	          };
	          data = JSON.stringify(d);
	        }
	        this.$http({
	          url: url,
	          method: "post",
	          headers: headers,
	          data: data
	        }).then(function (res) {
	          _this2.requestSucessFun(res);
	        }).catch(function (res) {
	          console.log("err:" + res.message);
	          _this2.$message({
	            showClose: true,
	            message: "UI模板获取结构数据失败",
	            type: "error"
	          });
	        });
	      }
	    },
	    geturlencode: function geturlencode(data) {
	      var tmp = Object.keys(data);
	      tmp.forEach(function (v, i) {
	        tmp[i] = v + "=" + data[v];
	      });
	      return tmp.join("&");
	    },
	    setData: function setData(key, value) {
	      if (this.tplData) this.tplData[key] = value;
	      if (this.comp) this.comp[key] = value;
	    },
	    setTableData: function setTableData(value) {
	      var tableModel = this.tableModel;
	      if (!tableModel) tableModel = "ifxbptxempxlatxetaxblxemxodxel";
	      this.setData(tableModel, value);
	    },
	    setFormData: function setFormData(value) {
	      var formModel = this.formModel;
	      if (!formModel) formModel = "ifxbptxempxlatxefoxrmxemxodxel";
	      this.setData(formModel, value);
	    },
	    getData: function getData(key) {
	      if (this.comp) return this.comp[key];
	      return null;
	    },
	    getTableData: function getTableData() {
	      return this.getData(this.tableModel);
	    },
	    getFormData: function getFormData() {
	      return this.getData(this.formModel);
	    },
	    getTableComp: function getTableComp() {
	      return this.comp.getTableComp();
	    },
	    getFormComp: function getFormComp() {
	      return this.comp.getFormComp();
	    },
	    getTableFormComp: function getTableFormComp() {
	      return this.comp.getTableFormComp();
	    },
	    validate: function validate(callback, type) {
	      this.comp.validate(callback, type);
	    },
	    resetFormData: function resetFormData(type) {
	      this.comp.resetFormData(type);
	    },
	    getNodeById: function getNodeById($node, id) {
	      if (id) {
	        if ($node.attr("_id") === id) {
	          return $node;
	        } else {
	          return $node.find("[_id=" + id + "]");
	        }
	      }
	      return null;
	    },
	    getTableDom: function getTableDom($node) {
	      if ($node) {
	        return $node.find("el-table");
	      }
	    },
	    getFormDom: function getFormDom($node) {
	      if ($node) {
	        return $node.find("el-form");
	      }
	    },
	    getTableIndexHtml: function getTableIndexHtml() {
	      return '<el-table-column type="index" width="50"></el-table-column>';
	    },
	    getTableMultiHtml: function getTableMultiHtml() {
	      return '<el-table-column type="selection" width="50"></el-table-column>';
	    },
	    getBaseTableOperateHtml: function getBaseTableOperateHtml(operateArr, fixed) {
	      var baseOperateArr = [{
	        title: "编辑",
	        icon: "edit"
	      }, {
	        title: "删除",
	        icon: "delete"
	      }];
	      var nowOperateArr = operateArr ? baseOperateArr.concat(operateArr) : baseOperateArr;
	      return this.getTableOperateHtml(nowOperateArr, fixed);
	    },
	    getTableOperateHtml: function getTableOperateHtml(operateArr, fixed) {
	      var html = "",
	          fixedStr = "";
	      if (operateArr.length > 0) {
	        if (fixed) {
	          fixedStr = ' fixed="' + fixed + '"';
	        }
	        html = '<el-table-column label="" width="150" ' + fixedStr + '>  <template scope="scope"> ';
	        for (var i = 0; i < operateArr.length; i++) {
	          var oper = operateArr[i];
	          var vifStr = "";
	          if (oper.vif) {
	            vifStr = 'v-if="' + oper.vif + '"';
	          }
	          if (oper.click) {
	            html += '<i @click="' + oper.click + '(scope)" ' + vifStr + ' class="el-icon-' + oper.icon + ' el-table-operate-icon"  title="' + oper.title + '"></i>';
	          } else {
	            html += "<i @click=\"handleClick('" + oper.icon + "',scope)\" " + vifStr + ' class="el-icon-' + oper.icon + ' el-table-operate-icon"  title="' + oper.title + '"></i>';
	          }
	        }
	        html += "</template></el-table-column>";
	      }
	      return html;
	    }
	  }
	}; //
	//
	//
	//
	//

/***/ }),

/***/ 335:
/***/ (function(module, exports) {

	'use strict';

	exports.__esModule = true;
	exports.default = {
	  methods: {
	    getSelectHasOptions4kangfu: function getSelectHasOptions4kangfu($dom, options, changeFun) {
	      $dom.attr('filterable', '');
	      $dom.attr('clearable', '');
	      $dom.attr(':allow-create', true);
	      $dom.attr('v-on:change', changeFun);
	      $dom[0].innerHTML = '<el-option v-for="item in ' + options + '" :item="item" :key="item.label" :label="item.label":value="item.label"></el-option>';
	    },
	    getSelectNoOptions4kangfu: function getSelectNoOptions4kangfu($dom, options, changeFun, remoteMethod) {
	      $dom.attr('filterable', '');
	      $dom.attr('clearable', '');
	      $dom.attr(':allow-create', true);
	      $dom.attr('remote', '');
	      $dom.attr(':remote-method', remoteMethod);
	      $dom.attr('v-on:change', changeFun);
	      $dom[0].innerHTML = '<el-option v-for="item in ' + options + '" :item="item" :key="item.label" :label="item.label":value="item.label"></el-option>';
	    },
	    getfilterSelectDom4kangfu: function getfilterSelectDom4kangfu($dom, options, changeFun, blurFun) {
	      $dom.attr('filterable', '');
	      $dom.attr('clearable', '');
	      $dom.attr(':allow-create', false);
	      $dom.attr('remote', '');
	      $dom.attr(':remote-method', 'shjdsansbah'); // 这个是随便填的值，不要去实现此方法
	      $dom.attr('v-on:change', changeFun);
	      $dom.attr('v-on:blur', blurFun);
	      $dom[0].innerHTML = '<el-option v-for="item in ' + options + '" :item="item" :key="item.label" :label="item.label":value="item.label"></el-option>';
	    }
	  }
	};

/***/ }),

/***/ 336:
/***/ (function(module, exports) {

	module.exports={render:function (){var _vm=this;var _h=_vm.$createElement;var _c=_vm._self._c||_h;
	  return _c('div', {
	    staticClass: "yon-uitemplate"
	  }, [_c('div', {
	    attrs: {
	      "id": 'el-template' + this.tplId
	    }
	  })])
	},staticRenderFns: []}

/***/ })

/******/ });