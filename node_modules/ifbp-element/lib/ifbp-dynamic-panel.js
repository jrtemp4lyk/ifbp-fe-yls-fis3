module.exports =
/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/dist/";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ({

/***/ 0:
/***/ (function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(277);


/***/ }),

/***/ 3:
/***/ (function(module, exports) {

	/* globals __VUE_SSR_CONTEXT__ */

	// this module is a runtime utility for cleaner component module output and will
	// be included in the final webpack user bundle

	module.exports = function normalizeComponent (
	  rawScriptExports,
	  compiledTemplate,
	  injectStyles,
	  scopeId,
	  moduleIdentifier /* server only */
	) {
	  var esModule
	  var scriptExports = rawScriptExports = rawScriptExports || {}

	  // ES6 modules interop
	  var type = typeof rawScriptExports.default
	  if (type === 'object' || type === 'function') {
	    esModule = rawScriptExports
	    scriptExports = rawScriptExports.default
	  }

	  // Vue.extend constructor export interop
	  var options = typeof scriptExports === 'function'
	    ? scriptExports.options
	    : scriptExports

	  // render functions
	  if (compiledTemplate) {
	    options.render = compiledTemplate.render
	    options.staticRenderFns = compiledTemplate.staticRenderFns
	  }

	  // scopedId
	  if (scopeId) {
	    options._scopeId = scopeId
	  }

	  var hook
	  if (moduleIdentifier) { // server build
	    hook = function (context) {
	      // 2.3 injection
	      context =
	        context || // cached call
	        (this.$vnode && this.$vnode.ssrContext) || // stateful
	        (this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext) // functional
	      // 2.2 with runInNewContext: true
	      if (!context && typeof __VUE_SSR_CONTEXT__ !== 'undefined') {
	        context = __VUE_SSR_CONTEXT__
	      }
	      // inject component styles
	      if (injectStyles) {
	        injectStyles.call(this, context)
	      }
	      // register component module identifier for async chunk inferrence
	      if (context && context._registeredComponents) {
	        context._registeredComponents.add(moduleIdentifier)
	      }
	    }
	    // used by ssr in case component is cached and beforeCreate
	    // never gets called
	    options._ssrRegister = hook
	  } else if (injectStyles) {
	    hook = injectStyles
	  }

	  if (hook) {
	    var functional = options.functional
	    var existing = functional
	      ? options.render
	      : options.beforeCreate
	    if (!functional) {
	      // inject component registration as beforeCreate hook
	      options.beforeCreate = existing
	        ? [].concat(existing, hook)
	        : [hook]
	    } else {
	      // register for functioal component in vue file
	      options.render = function renderWithStyleInjection (h, context) {
	        hook.call(context)
	        return existing(h, context)
	      }
	    }
	  }

	  return {
	    esModule: esModule,
	    exports: scriptExports,
	    options: options
	  }
	}


/***/ }),

/***/ 10:
/***/ (function(module, exports) {

	/*
		MIT License http://www.opensource.org/licenses/mit-license.php
		Author Tobias Koppers @sokra
	*/
	// css base code, injected by the css-loader
	module.exports = function() {
		var list = [];

		// return the list of modules as css string
		list.toString = function toString() {
			var result = [];
			for(var i = 0; i < this.length; i++) {
				var item = this[i];
				if(item[2]) {
					result.push("@media " + item[2] + "{" + item[1] + "}");
				} else {
					result.push(item[1]);
				}
			}
			return result.join("");
		};

		// import a list of modules into the list
		list.i = function(modules, mediaQuery) {
			if(typeof modules === "string")
				modules = [[null, modules, ""]];
			var alreadyImportedModules = {};
			for(var i = 0; i < this.length; i++) {
				var id = this[i][0];
				if(typeof id === "number")
					alreadyImportedModules[id] = true;
			}
			for(i = 0; i < modules.length; i++) {
				var item = modules[i];
				// skip already imported module
				// this implementation is not 100% perfect for weird media query combinations
				//  when a module is imported multiple times with different media queries.
				//  I hope this will never occur (Hey this way we have smaller bundles)
				if(typeof item[0] !== "number" || !alreadyImportedModules[item[0]]) {
					if(mediaQuery && !item[2]) {
						item[2] = mediaQuery;
					} else if(mediaQuery) {
						item[2] = "(" + item[2] + ") and (" + mediaQuery + ")";
					}
					list.push(item);
				}
			}
		};
		return list;
	};


/***/ }),

/***/ 11:
/***/ (function(module, exports, __webpack_require__) {

	/*
	  MIT License http://www.opensource.org/licenses/mit-license.php
	  Author Tobias Koppers @sokra
	  Modified by Evan You @yyx990803
	*/

	var hasDocument = typeof document !== 'undefined'

	if (false) {
	  if (!hasDocument) {
	    throw new Error(
	    'vue-style-loader cannot be used in a non-browser environment. ' +
	    "Use { target: 'node' } in your Webpack config to indicate a server-rendering environment."
	  ) }
	}

	var listToStyles = __webpack_require__(12)

	/*
	type StyleObject = {
	  id: number;
	  parts: Array<StyleObjectPart>
	}

	type StyleObjectPart = {
	  css: string;
	  media: string;
	  sourceMap: ?string
	}
	*/

	var stylesInDom = {/*
	  [id: number]: {
	    id: number,
	    refs: number,
	    parts: Array<(obj?: StyleObjectPart) => void>
	  }
	*/}

	var head = hasDocument && (document.head || document.getElementsByTagName('head')[0])
	var singletonElement = null
	var singletonCounter = 0
	var isProduction = false
	var noop = function () {}

	// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
	// tags it will allow on a page
	var isOldIE = typeof navigator !== 'undefined' && /msie [6-9]\b/.test(navigator.userAgent.toLowerCase())

	module.exports = function (parentId, list, _isProduction) {
	  isProduction = _isProduction

	  var styles = listToStyles(parentId, list)
	  addStylesToDom(styles)

	  return function update (newList) {
	    var mayRemove = []
	    for (var i = 0; i < styles.length; i++) {
	      var item = styles[i]
	      var domStyle = stylesInDom[item.id]
	      domStyle.refs--
	      mayRemove.push(domStyle)
	    }
	    if (newList) {
	      styles = listToStyles(parentId, newList)
	      addStylesToDom(styles)
	    } else {
	      styles = []
	    }
	    for (var i = 0; i < mayRemove.length; i++) {
	      var domStyle = mayRemove[i]
	      if (domStyle.refs === 0) {
	        for (var j = 0; j < domStyle.parts.length; j++) {
	          domStyle.parts[j]()
	        }
	        delete stylesInDom[domStyle.id]
	      }
	    }
	  }
	}

	function addStylesToDom (styles /* Array<StyleObject> */) {
	  for (var i = 0; i < styles.length; i++) {
	    var item = styles[i]
	    var domStyle = stylesInDom[item.id]
	    if (domStyle) {
	      domStyle.refs++
	      for (var j = 0; j < domStyle.parts.length; j++) {
	        domStyle.parts[j](item.parts[j])
	      }
	      for (; j < item.parts.length; j++) {
	        domStyle.parts.push(addStyle(item.parts[j]))
	      }
	      if (domStyle.parts.length > item.parts.length) {
	        domStyle.parts.length = item.parts.length
	      }
	    } else {
	      var parts = []
	      for (var j = 0; j < item.parts.length; j++) {
	        parts.push(addStyle(item.parts[j]))
	      }
	      stylesInDom[item.id] = { id: item.id, refs: 1, parts: parts }
	    }
	  }
	}

	function createStyleElement () {
	  var styleElement = document.createElement('style')
	  styleElement.type = 'text/css'
	  head.appendChild(styleElement)
	  return styleElement
	}

	function addStyle (obj /* StyleObjectPart */) {
	  var update, remove
	  var styleElement = document.querySelector('style[data-vue-ssr-id~="' + obj.id + '"]')

	  if (styleElement) {
	    if (isProduction) {
	      // has SSR styles and in production mode.
	      // simply do nothing.
	      return noop
	    } else {
	      // has SSR styles but in dev mode.
	      // for some reason Chrome can't handle source map in server-rendered
	      // style tags - source maps in <style> only works if the style tag is
	      // created and inserted dynamically. So we remove the server rendered
	      // styles and inject new ones.
	      styleElement.parentNode.removeChild(styleElement)
	    }
	  }

	  if (isOldIE) {
	    // use singleton mode for IE9.
	    var styleIndex = singletonCounter++
	    styleElement = singletonElement || (singletonElement = createStyleElement())
	    update = applyToSingletonTag.bind(null, styleElement, styleIndex, false)
	    remove = applyToSingletonTag.bind(null, styleElement, styleIndex, true)
	  } else {
	    // use multi-style-tag mode in all other cases
	    styleElement = createStyleElement()
	    update = applyToTag.bind(null, styleElement)
	    remove = function () {
	      styleElement.parentNode.removeChild(styleElement)
	    }
	  }

	  update(obj)

	  return function updateStyle (newObj /* StyleObjectPart */) {
	    if (newObj) {
	      if (newObj.css === obj.css &&
	          newObj.media === obj.media &&
	          newObj.sourceMap === obj.sourceMap) {
	        return
	      }
	      update(obj = newObj)
	    } else {
	      remove()
	    }
	  }
	}

	var replaceText = (function () {
	  var textStore = []

	  return function (index, replacement) {
	    textStore[index] = replacement
	    return textStore.filter(Boolean).join('\n')
	  }
	})()

	function applyToSingletonTag (styleElement, index, remove, obj) {
	  var css = remove ? '' : obj.css

	  if (styleElement.styleSheet) {
	    styleElement.styleSheet.cssText = replaceText(index, css)
	  } else {
	    var cssNode = document.createTextNode(css)
	    var childNodes = styleElement.childNodes
	    if (childNodes[index]) styleElement.removeChild(childNodes[index])
	    if (childNodes.length) {
	      styleElement.insertBefore(cssNode, childNodes[index])
	    } else {
	      styleElement.appendChild(cssNode)
	    }
	  }
	}

	function applyToTag (styleElement, obj) {
	  var css = obj.css
	  var media = obj.media
	  var sourceMap = obj.sourceMap

	  if (media) {
	    styleElement.setAttribute('media', media)
	  }

	  if (sourceMap) {
	    // https://developer.chrome.com/devtools/docs/javascript-debugging
	    // this makes source maps inside style tags work properly in Chrome
	    css += '\n/*# sourceURL=' + sourceMap.sources[0] + ' */'
	    // http://stackoverflow.com/a/26603875
	    css += '\n/*# sourceMappingURL=data:application/json;base64,' + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + ' */'
	  }

	  if (styleElement.styleSheet) {
	    styleElement.styleSheet.cssText = css
	  } else {
	    while (styleElement.firstChild) {
	      styleElement.removeChild(styleElement.firstChild)
	    }
	    styleElement.appendChild(document.createTextNode(css))
	  }
	}


/***/ }),

/***/ 12:
/***/ (function(module, exports) {

	/**
	 * Translates the list format produced by css-loader into something
	 * easier to manipulate.
	 */
	module.exports = function listToStyles (parentId, list) {
	  var styles = []
	  var newStyles = {}
	  for (var i = 0; i < list.length; i++) {
	    var item = list[i]
	    var id = item[0]
	    var css = item[1]
	    var media = item[2]
	    var sourceMap = item[3]
	    var part = {
	      id: parentId + ':' + i,
	      css: css,
	      media: media,
	      sourceMap: sourceMap
	    }
	    if (!newStyles[id]) {
	      styles.push(newStyles[id] = { id: id, parts: [part] })
	    } else {
	      newStyles[id].parts.push(part)
	    }
	  }
	  return styles
	}


/***/ }),

/***/ 277:
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	var _ifbpDynamicPanel = __webpack_require__(278);

	var _ifbpDynamicPanel2 = _interopRequireDefault(_ifbpDynamicPanel);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	_ifbpDynamicPanel2.default.install = function (Vue) {
	  Vue.component(_ifbpDynamicPanel2.default.name, _ifbpDynamicPanel2.default);
	};

	exports.default = _ifbpDynamicPanel2.default;

/***/ }),

/***/ 278:
/***/ (function(module, exports, __webpack_require__) {

	function injectStyle (ssrContext) {
	  __webpack_require__(279)
	}
	var Component = __webpack_require__(3)(
	  /* script */
	  __webpack_require__(281),
	  /* template */
	  __webpack_require__(282),
	  /* styles */
	  injectStyle,
	  /* scopeId */
	  null,
	  /* moduleIdentifier (server only) */
	  null
	)

	module.exports = Component.exports


/***/ }),

/***/ 279:
/***/ (function(module, exports, __webpack_require__) {

	// style-loader: Adds some css to the DOM by adding a <style> tag

	// load the styles
	var content = __webpack_require__(280);
	if(typeof content === 'string') content = [[module.id, content, '']];
	if(content.locals) module.exports = content.locals;
	// add the styles to the DOM
	var update = __webpack_require__(11)("4deb2af6", content, true);

/***/ }),

/***/ 280:
/***/ (function(module, exports, __webpack_require__) {

	exports = module.exports = __webpack_require__(10)();
	// imports


	// module
	exports.push([module.id, "\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n", ""]);

	// exports


/***/ }),

/***/ 281:
/***/ (function(module, exports) {

	'use strict';

	exports.__esModule = true;
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//
	//

	exports.default = {
	  name: 'IfbpDynamicPanel',
	  data: function data() {
	    var _this = this;

	    return {
	      childIcons: [{
	        icon: 'plus',
	        click: function click(e) {
	          var parentDom = e.currentTarget.parentNode.parentNode;
	          if (parentDom) {
	            var index = parentDom.getAttribute('data-index');
	          } else {
	            return;
	          }
	          if (!_this.dynamicId) {
	            _this.$message({
	              message: '请先保存主表信息',
	              type: 'error'
	            });
	          } else {
	            // 关闭table中的编辑区
	            _this.$refs['dynamic' + index][0].getTableComp().closeExpandRow();
	            // 重置新增数据
	            _this.$refs['dynamic' + index][0].resetFormData();
	            // 显示新增区域
	            _this.$refs['dynamic' + index][0].formShow = true;
	          }
	        }
	      }],
	      dynamicData: {},
	      childBaseData: {},
	      childDelData: {},
	      childDelDialogVisible: false,
	      typicalDemoChildResetFun: function typicalDemoChildResetFun($node) {
	        var $table = this.getTableDom($node);
	        var $operateHtml = this.getBaseTableOperateHtml();
	        $table.append($operateHtml);
	        return $node[0].outerHTML;
	      }
	    };
	  },

	  props: ['dynamicPanelData', 'isCreate', 'dynamicId'],
	  watch: {
	    dynamicPanelData: function dynamicPanelData(val) {
	      // var dynamicTplData = {};
	      for (var i = 0; i < val.length; i++) {
	        // dynamicTplData[={};
	        debugger;
	        this.dynamicData[val[i].id] = {};
	      }
	    }
	  },
	  methods: {
	    childEditClick: function childEditClick(item, index, scope) {
	      this.$refs['dynamic' + index][0].getTableComp().expandRow(scope.row);
	      this.$refs['dynamic' + index][0].formShow = false;
	      this.$refs['dynamic' + index][0].setFormData(scope.row);
	      // 备份数据
	      this.childBaseData = JSON.parse(JSON.stringify(scope.row));
	      this.childEditIndex = scope.$index;
	    },
	    childDeleteClick: function childDeleteClick(item, index, scope) {
	      this.childDelDialogVisible = true;
	      this.childDelData = scope.row;
	      this.childIndex = index;
	    },
	    childDeleteDialogClick: function childDeleteDialogClick() {
	      var _this2 = this;

	      var data = this.childDelData;
	      this.$http({
	        url: this.dynamicPanelData[this.childIndex].deleteURL,
	        method: 'post',
	        data: data,
	        headers: {
	          'Content-Type': 'application/json'
	        },
	        dataType: 'application/json'
	      }).then(function (res) {
	        _this2.$emit('requestDynamic', res.data);
	        if (res.data.success === true) {
	          _this2.$message({
	            message: '删除成功',
	            type: 'success'
	          });
	          _this2.childDelDialogVisible = false;
	          _this2.dynamicRequest();
	        } else {
	          _this2.$message({
	            message: res.data.error.errorMessage,
	            type: 'error'
	          });
	        }
	      }).catch(function () {
	        _this2.$message({
	          message: '删除失败',
	          type: 'error'
	        });
	      });
	    },
	    childFormConfirm: function childFormConfirm(item, index, type) {
	      var _this3 = this;

	      var vm = this;
	      var url = this.dynamicPanelData[index].updateURL;
	      this.$refs['dynamic' + index][0].validate(function (valid) {
	        if (valid) {
	          var data = vm.$refs['dynamic' + index][0].getFormData();
	          // 拼接主表的id
	          data = $.extend(data, {
	            pk_example: vm.dynamicId
	          });
	          if (type === 'form') {
	            url = _this3.dynamicPanelData[index].saveURL;
	          }
	          vm.$http({
	            url: url,
	            method: 'post',
	            data: data,
	            headers: {
	              'Content-Type': 'application/json'
	            },
	            dataType: 'application/json'
	          }).then(function (res) {
	            if (res.data.success) {
	              vm.$message({
	                message: '保存成功'
	              });
	              vm.$refs['dynamic' + index][0].tableShow = true;
	              // 隐藏新增区域
	              vm.$refs['dynamic' + index][0].formShow = false;
	              // 关闭Table中的编辑区域
	              vm.$refs['dynamic' + index][0].getTableComp().closeExpandRow();
	              if (type === 'form') {
	                vm.dynamicRequest();
	              }
	            } else {
	              vm.$message({
	                message: res.data.error.errorMessage,
	                type: 'error'
	              });
	            }
	          }).catch(function () {
	            vm.$message({
	              message: '保存失败',
	              type: 'error'
	            });
	          });
	        } else {
	          vm.$message('校验未通过');
	        }
	      }, type);
	      this.$emit('FormConfirm');
	    },
	    childFormCancel: function childFormCancel(index, type) {
	      // console.log(index);
	      if (type === 'form') {
	        this.$refs['dynamic' + index][0].formShow = false;
	      } else {
	        this.$refs['dynamic' + index][0].getTableComp().closeExpandRow();
	        var childT = this.$refs['dynamic' + index][0].getTableData();
	        childT[this.childEditIndex] = this.childBaseData;
	        this.$refs['dynamic' + index][0].setTableData(childT);
	      }
	      this.$emit('FormCancel');
	    },
	    dynamicRequest: function dynamicRequest() {
	      var _this4 = this;

	      if (!this.dynamicId) {
	        this.$nextTick(function () {
	          for (var item in _this4.$refs) {
	            if (item.indexOf('dynamic') > -1) {
	              _this4.$refs[item][0].tableShow = false;
	            }
	          }
	        });
	        return;
	      }
	      var url;

	      var _loop = function _loop(i) {
	        // debugger;
	        var resData = void 0;
	        data = {
	          searchParams: {
	            searchMap: {
	              pk_example: _this4.dynamicId
	            }
	          }
	        };

	        _this4.$http({
	          url: _this4.dynamicPanelData[i].queryURL,
	          method: 'post',
	          data: data,
	          dataType: 'json'
	        }).then(function (res) {
	          if (res.data.success === true) {
	            resData = res.data.data.content;
	            _this4.$nextTick(function () {
	              if (_this4.$refs['dynamic' + i][0]) {
	                _this4.$refs['dynamic' + i][0].setTableData(resData);
	              } else {
	                _this4.$refs['dynamic' + i][0].$set(_this4.dynamicData[_this4.dynamicPanelData[i].id], 'uitemplateTableData', resData);
	              }
	              if (resData.length < 1) {
	                _this4.$refs['dynamic' + i][0].tableShow = false;
	              } else {
	                _this4.$refs['dynamic' + i][0].tableShow = true;
	              }
	              // this.$refs['dynamic' + i][0].tableShow = resData.length < 1 ? false : true;
	            });
	          } else {
	            _this4.$message({
	              message: res.data.msg,
	              type: 'error'
	            });
	          }
	        }).catch(function (e) {
	          _this4.$message({
	            message: '子表信息获取失败',
	            type: 'error'
	          });
	        });
	      };

	      for (var i = 0; i < this.dynamicPanelData.length; i++) {
	        var data;

	        _loop(i);
	      }
	    }
	  },
	  mounted: function mounted() {
	    this.dynamicRequest();
	  },
	  created: function created() {
	    for (var i = 0; i < this.dynamicPanelData.length; i++) {
	      this.dynamicData[this.dynamicPanelData[i].id] = {};
	    }
	  }
	};

/***/ }),

/***/ 282:
/***/ (function(module, exports) {

	module.exports={render:function (){var _vm=this;var _h=_vm.$createElement;var _c=_vm._self._c||_h;
	  return _c('div', {}, _vm._l((_vm.dynamicPanelData), function(item, index) {
	    return _c('div', {}, [_c('ifbp-panel', {
	      attrs: {
	        "title": item.title,
	        "icons": _vm.childIcons,
	        "data-index": index
	      }
	    }, [_c('ifbp-template', {
	      ref: 'dynamic' + index,
	      refInFor: true,
	      attrs: {
	        "tplId": 'dynamic_' + item.id,
	        "pk-temp": item.pkTemp,
	        "funnode": item.funnode,
	        "nexuskey": item.nexuskey,
	        "show-type": "table-form",
	        "tpl-data": _vm.dynamicData[item.id],
	        "tpl-reset-fun": _vm.typicalDemoChildResetFun
	      },
	      on: {
	        "edit-table-click": function(scope) {
	          _vm.childEditClick(item, index, scope)
	        },
	        "delete-table-click": function(scope) {
	          _vm.childDeleteClick(item, index, scope)
	        },
	        "form-confirm-click": function(type) {
	          _vm.childFormConfirm(item, index, type)
	        },
	        "form-cancel-click": function(type) {
	          _vm.childFormCancel(index, type)
	        }
	      }
	    }), _c('ifbp-del-dialog', {
	      attrs: {
	        "message": "确认删除该数据？删除后无法恢复。"
	      },
	      on: {
	        "click": _vm.childDeleteDialogClick
	      },
	      model: {
	        value: (_vm.childDelDialogVisible),
	        callback: function($$v) {
	          _vm.childDelDialogVisible = $$v
	        },
	        expression: "childDelDialogVisible"
	      }
	    })], 1)], 1)
	  }))
	},staticRenderFns: []}

/***/ })

/******/ });